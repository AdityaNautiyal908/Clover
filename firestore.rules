rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /questions/{id} {
      allow read: if true;      // anyone can fetch questions
      allow write: if true;     // allow writes for development/testing
    }
  }
}
